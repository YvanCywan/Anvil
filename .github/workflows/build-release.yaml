name: Release Anvil

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  calculate-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.semver.outputs.next_version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Calculate Version
        id: semver
        run: |
          COMMIT_COUNT=$(git rev-list --count HEAD)
          echo "next_version=0.1.$COMMIT_COUNT" >> $GITHUB_OUTPUT

  build-and-release:
    needs: calculate-version
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: anvil-linux-x64.zip
            binary_name: anvil
            anvilw: anvilw
          - os: macos-latest
            artifact_name: anvil-macos-arm64.zip
            binary_name: anvil
            anvilw: anvilw
          - os: windows-latest
            artifact_name: anvil-windows-x64.zip
            binary_name: anvil.exe
            anvilw: anvilw.bat

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Anvil Build
        run: |
          ./${{ matrix.anvilw }} build
        shell: bash

      - name: Package Release
        run: |
          mkdir staging
          mkdir staging/bin
          mkdir staging/include
          
          # Copy the binary
          cp bin/${{ matrix.binary_name }} staging/bin/
          
          # Copy source files to include directory for distribution
          cp -r src/* staging/include/
          
          cd staging

          # Zip it up based on the OS
          if [ "${{ runner.os }}" == "Windows" ]; then
            7z a ../${{ matrix.artifact_name }} *
          else
            zip -r ../${{ matrix.artifact_name }} *
          fi
        shell: bash

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_name }}

  publish-release:
    needs: [ calculate-version, build-and-release ]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          # This downloads all artifacts from the 'build-and-release' job
          path: release_assets
          # CRITICAL FIX: This flattens the files.
          # You will get 'release_assets/anvil-linux-x64.zip', etc. directly.
          merge-multiple: true

      - name: Verify Files
        run: ls -l release_assets

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ needs.calculate-version.outputs.version }}
          name: Release v${{ needs.calculate-version.outputs.version }}
          files: release_assets/*
          fail_on_unmatched_files: true
          make_latest: true
          generate_release_notes: true
